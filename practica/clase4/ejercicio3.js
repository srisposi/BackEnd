const express = require("express");
const app = express();
const PORT = 8080;

//===================Parte I=============================

//===================Parte II============================
//=======================================================
app.listen(PORT, () => {
  console.log(`Estamos escuchando: http://localhost:${PORT}`);
});
